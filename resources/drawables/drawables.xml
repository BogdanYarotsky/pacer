<drawables>
    <bitmap id="LauncherIcon" filename="launcher_icon.png" />
    <!-- The release build's bottom-slot mark; both PNGs come from
         tools/make-icons.ps1, never from hand editing. -->
    <bitmap id="LogoSmall" filename="logo_small.png" />
</drawables>
